services:
  plannerservice:
    image: ${WEBAPI_IMAGE:-plannerservice:latest}
    env_file:
      - ./.env
    environment:
      ASPNETCORE_URLS: http://0.0.0.0:8080
    ports:
      - "8080:8080"
    restart: unless-stopped
